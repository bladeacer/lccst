[project]
name = "python-http-server"
version = "1.0.0"
description = "Skill-guided Python HTTP server with CRUD, validation, and rate limiting"
requires-python = ">=3.10"

[dependency-groups]
dev = ["pytest>=8"]
